# This file tells Git which files shouldn't be added to source control

.DS_Store
.idea
.env
.env.*
dist/
node_modules
storage/

# Rebuildable from sites/ via `npm run db`. The hand-edited columns live in
# tags.csv, which IS committed.
archive.db
archive.db-wal
archive.db-shm
